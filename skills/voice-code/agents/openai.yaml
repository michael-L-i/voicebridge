interface:
  display_name: "Voice Code"
  short_description: "Start a local voice conversation with Codex"
  default_prompt: "Use $voice-code to start a local voice conversation."

policy:
  allow_implicit_invocation: false
