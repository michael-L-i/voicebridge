interface:
  display_name: "Interrupt Voice Code"
  short_description: "Interrupt and add spoken guidance"
  default_prompt: "Use $voice-interrupt to stop current audio and add spoken guidance."

policy:
  allow_implicit_invocation: false
