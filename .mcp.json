{
  "mcpServers": {
    "voicebridge": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/voicebridge-mcp-bootstrap",
      "env": {
        "VOICEBRIDGE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
