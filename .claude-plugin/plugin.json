{
  "name": "voicebridge",
  "version": "0.2.1",
  "description": "A fully local voice companion for Claude Code on Apple Silicon -- talk to Claude instead of typing, with no cloud API keys.",
  "author": {
    "name": "Michael Li"
  },
  "license": "MIT",
  "mcpServers": {
    "voicebridge": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/voicebridge-mcp-bootstrap",
      "env": {
        "VOICEBRIDGE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
